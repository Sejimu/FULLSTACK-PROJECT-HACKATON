import { Typography } from "@mui/material";
import React from "react";

function NotFoundPage() {
  return (
    <Typography color={"error"} mt={5} variant="h3">
      NOT FOUND
    </Typography>
  );
}

export default NotFoundPage;
